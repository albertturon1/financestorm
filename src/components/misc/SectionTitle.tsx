const SectionTitle = ({ children }: { children: string }) => (
  <h1 className="text-lg font-extrabold lowercase text-dark_navy underline underline-offset-2 sm:text-xl">
    {children}
  </h1>
);

export default SectionTitle;
